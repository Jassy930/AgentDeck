# AgentDeck 本地环境变量示例。
# 复制为 .env（真实 .env 已被 .gitignore 忽略，不会入库）。
#
# 加载规则：GUI 进程启动时从当前工作目录读取 .env（`swift run` 时即仓库根）。
# 真实 shell 环境变量优先——.env 只填补“未设”的键，不会覆盖你 export 或命令行内联的值。
# 支持 `KEY=VALUE`、`export KEY=VALUE`、`# 注释`、以及包裹的成对引号。

# preview/gallery 窗口开在第几块屏（1-based，与 `screencapture -D N` 一致）。默认关闭。
# AGENTDECK_PREVIEW_SCREEN=2

# 运行 profile（stable / dev）。通常用 `--profile` 传，一般不必设。
# AGENTDECK_PROFILE=dev

# 运行记录 / 诊断数据目录（默认 ~/Library/Application Support/AgentDeck/）。
# AGENTDECK_DATA_DIR=/absolute/path/to/data
