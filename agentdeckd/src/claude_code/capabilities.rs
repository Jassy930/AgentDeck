//! Claude Code SessionCapabilities builder + probes.
//!
//! Populated in Task 4B. Task 4A ships a placeholder via
//! `ClaudeCodeAdapter::capabilities` so the daemon can satisfy the N7
//! invariant ("SessionCapabilities before any AgentItem") even while the
//! real probe is not yet wired.
